# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Profiling dumps
*.prof

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env file
.env

# Editor/IDE
# .idea/
# .vscode/

# Mimosa 安全扫描产物
.mimosa/

# 提交信息草稿（git 是原生 Windows 二进制，喂长提交信息时先落成文件）
_commitmsg.txt

# WorkBuddy 内部工作笔记（逐日日志、MEMORY.md、gfx-invariants 等）—— 不对外公开。
# 注意：只是不再跟踪；若要让它们彻底从 git 历史里消失，必须重写历史（见 memory 台账）。
.workbuddy/

# Agent 内部工作文档（原 docs/ 下的过程性材料）—— 不对外公开，永久排除。
# 2026-09-21 从 docs/ 迁出：技术选型（gui-*-options / runtime-gui-split-options）、
# 开发计划与提示词（mobile-port-plan / gui-prompts）、现状台账与缺口清单
# （gui-component-status / undecided-and-unimplemented）。
# 这些是**未定稿**的过程记录，随时会被推翻，不适合作为对外承诺，故不再跟踪。
# docs/ 里留下的是一律**已定稿的对外手册**（官网、README、npm 包页、gox create 模板都引用它们）。
#
# ⚠️ 重要：迁移时已同步清掉所有指向这些文件的公开链接（README / website / docs 内部交叉引用）
#    与 gfx 的 unknown-tag 警告文案 —— 否则线上会出现 404 而 check-site.py 查不出来
#    （它只校验站内相对链接，外链 github.com/... 一律跳过）。
agent_doc/

/.zcode/

# 构建产物：交叉编译出来的二进制与 npm 打包中间物都落在 dist/（scripts/*.sh 产出）
dist/

# ─────────────────────────────────────────────────────────────────────────────
# 子模块（2026-09-24 从本仓库拆出去，各自成为独立仓库）
#
#   npm/                → https://github.com/14752222/gox-npm
#   website/            → https://github.com/14752222/gox-website
#   gox-logo-concepts/  → https://github.com/14752222/gox-logo-concepts
#
# ⚠️ 永远不要把这三条路径（或它们下面的任何路径）写进这份 .gitignore。
#    子模块在父仓库里是一个必须可跟踪的 gitlink：路径一旦被忽略，
#    `git submodule add` 会被拒绝，已存在的指针也会在提交时被静默丢掉。
#
# ⚠️ 原先写在这里的 `npm/*.tgz` 与 `!npm/binaries/**/*.exe` 两条规则已随拆分作废 ——
#    它们防的是「这份 .gitignore 会被 npm 打包当成忽略规则读走」，而拆出去之后
#    包目录里读到的是 gox-npm 仓库自己的 .gitignore。完整的教训说明
#    （2026-09-20：因为忽略规则把包内容吃掉，发过一个没有二进制的空壳包）
#    已随规则一起搬到 gox-npm/.gitignore。
#
# website/ 曾经长期被本文件排除、只在本地改，于是线上一直是那份旧快照（导航还写着
#    v0.1.1）—— 2026-09-21 收回版本库并加了 pages.yml 自动发布。现在它是子模块，
#    改完要在 website 仓库里提交推送，再回本仓库更新指针，两处都要推。
#
# 克隆方式：git clone --recurse-submodules git@github.com:14752222/Gox.git
# 已有克隆补子模块：git submodule update --init
# ─────────────────────────────────────────────────────────────────────────────
