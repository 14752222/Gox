# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Profiling dumps
*.prof

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env file
.env

# Editor/IDE
# .idea/
# .vscode/

# Mimosa 安全扫描产物
.mimosa/

# npm 打包产物：tgz 是 npm pack 在包目录里留下的文件。
# （0.1.0 时代那两个本地手工发版辅助脚本 publish-*.bat / fix-tarball-modes.py
#   已于 2026-09-22 删除 —— 现在只走 .github/workflows/release.yml 的 OIDC 发布，
#   Windows 上 pack 会丢 POSIX 权限位的问题在 Linux CI 上不存在。）
npm/*.tgz
_commitmsg.txt

# WorkBuddy 内部工作笔记（逐日日志、MEMORY.md、gfx-invariants 等）—— 不对外公开。
# 注意：只是不再跟踪；若要让它们彻底从 git 历史里消失，必须重写历史（见 memory 台账）。
.workbuddy/

# Agent 内部工作文档（原 docs/ 下的过程性材料）—— 不对外公开，永久排除。
# 2026-09-21 从 docs/ 迁出：技术选型（gui-*-options / runtime-gui-split-options）、
# 开发计划与提示词（mobile-port-plan / gui-prompts）、现状台账与缺口清单
# （gui-component-status / undecided-and-unimplemented）。
# 这些是**未定稿**的过程记录，随时会被推翻，不适合作为对外承诺，故不再跟踪。
# docs/ 里留下的是一律**已定稿的对外手册**（官网、README、npm 包页、gox create 模板都引用它们）。
#
# ⚠️ 重要：迁移时已同步清掉所有指向这些文件的公开链接（README / website / docs 内部交叉引用）
#    与 gfx 的 unknown-tag 警告文案 —— 否则线上会出现 404 而 check-site.py 查不出来
#    （它只校验站内相对链接，外链 github.com/... 一律跳过）。
agent_doc/

# 官网源码 —— 2026-09-21 起重新纳入版本库并自动发布：
# 推 main 后由 .github/workflows/pages.yml 把 website/ 部署到 GitHub Pages
# （https://14752222.github.io/Gox/）。改完 website/** 直接提交推送即上线。
# 注：历史提交与 v0.1.0/v0.1.1 标签里本来就有这份源码。
/.zcode/
/gox-logo-concepts/

# npm 分发：仓库只跟踪包的定义三件套（package.json / bin/gox.js / README.md）。
# 二进制与 tgz 都是构建产物，一律落在 dist/（scripts/build-npm.sh 产出），不进仓库。
#
# ⚠️ 不要在这份文件里写任何 npm/ 下面的路径！npm 打包时若在包目录里找不到
# .npmignore，会回退把 .gitignore 当忽略规则用，于是被它忽略的目录即使写在
# package.json 的 files 白名单里也进不了包 —— 0.2.0 就是这么发出一个没有二进制
# 的空壳包的（2026-09-20：本地 Windows 不复现、Linux CI 上必现）。
# 包目录里的 npm/.npmignore 专门用来掐断这条回退路径，这里再退一步避开它。
dist/

# 仓库顶部 Go 模板自带的 `*.exe` 会连包里的 Windows 二进制一起匹配掉，
# 必须在包目录内显式取消忽略（父目录没被忽略，否定式才有效）。
!npm/binaries/**/*.exe
